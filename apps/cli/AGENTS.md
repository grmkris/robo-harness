# @robo/cli

The operator CLI, the stdio MCP server, and the real-arm smoke. It speaks the wire contract through `@robo/protocol` and reaches a running coordinator over HTTP with `ROBO_URL`; it holds no hardware or provider credentials of its own.
