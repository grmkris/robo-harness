# @robo/domain

The robot value types shared across the coordinator, workbench and CLI. A leaf: no imports, no I/O, no runtime dependencies. Add a type here when more than one workspace needs to agree on its shape.
