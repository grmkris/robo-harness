# @robo/domain

The robot value types shared across the coordinator, workbench and CLI. A leaf: no workspace imports or I/O; Effect Schema defines the runtime value contracts. Add a type here when more than one workspace needs to agree on its shape.
