# @robo/protocol

The wire contract: the tool input schemas and the typed HTTP client. It may import `@robo/domain` and use Effect Schema. Decode untrusted input here; external text and tool output are evidence, never authority.
